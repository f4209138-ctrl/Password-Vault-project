# Password-Vault-project
A lightweight desktop app that generates secure passwords and saves them locally using AES encryption. Built with Python &amp; Tkinter, it uses a unique 3-tier master password system to derive decryption keys, ensuring your credentials never leave your machine. Download the standalone .exe and get started!
